#include<iostream>
#include<map>

using namespace std;

int main(){
    map<int, int> mp;
    mp.insert({1,1});
    // for(auto it = mp.begin(); it != mp.end(); it++){
    //     cout<<it->second;
    // }
    // bool a = mp.find(1);
    return 0;
}